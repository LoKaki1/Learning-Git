import { DataGrid } from "@mui/x-data-grid"

export default function ScannerWatchlist (props) {
    const columns  = {
        
    }

    return (
        <>
        <DataGrid/>
        
        </>
    )
}